#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../colortape/bin/colortape" "$@"
  ret=$?
else 
  node  "$basedir/../colortape/bin/colortape" "$@"
  ret=$?
fi
exit $ret
